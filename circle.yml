version: 2
jobs:
  build:
    machine: true
    steps:
      - checkout
      - run:
          name: Build
          command: docker build -t apinfo .
      - run: echo "ok"

workflows:
  version: 2
  build:
    jobs:
      - build

notify:
  webhooks:
    - url: https://samson.rdops.systems/integrations/circleci/00fda842a2d360df945321f71408db4e
